# Pong C++

### Installing & Usage

Ensure that SFML and Eigen are installed, as well as a recent `g++` version (tested with 9.20).

```
git clone https://github.com/kontheocharis/pong-cpp
cd ./pong-cpp
mkdir -p build && cd build
cmake ..
make
```

Then run with `./pong++`
